executorIdentity:
  id: protocol-lab-reference-http3-test-executor
  name: ProtocolLab Reference HTTP/3 Test Executor Fixture
  version: 0.1.0-fixture
  vendor: Incursa
versionCompatibility:
  contractVersion: test-executor-v1
  compatibleContractVersions:
    - test-executor-v1
  executorVersion: 0.1.0-fixture
claimedCapabilities:
  - id: http3.core.status
    status: supported
  - id: http3.payload.bytes.64kb
    status: supported
  - id: http3.upload.echo.64kb
    status: supported
supportedTestSelectors:
  - selectorType: test-id
    expression: http3.core.status
  - selectorType: test-id
    expression: http3.payload.bytes.64kb
  - selectorType: test-id
    expression: http3.upload.echo.64kb
supportedScenarioSelectors:
  - selectorType: scenario-id
    expression: http3.core.status
  - selectorType: scenario-id
    expression: http3.payload.bytes.64kb
  - selectorType: scenario-id
    expression: http3.upload.echo.64kb
supportedProtocolFamilies:
  - h3
supportedExecutionModes:
  - contract-declared
requiredTargetEndpointBindings:
  - bindingId: primary
    purpose: test-endpoint
    endpointType: http
    protocols:
      - h3
supportedArtifactTypes:
  - type: validation.json
    producedByStates:
      - stopped
      - failed
  - type: result.json
    producedByStates:
      - stopped
      - failed
  - type: load-generator.stdout.txt
    producedByStates:
      - stopped
      - failed
  - type: load-generator.stderr.txt
    producedByStates:
      - stopped
      - failed
metricsAvailability:
  available: true
  availableKinds:
    - session
    - validation
telemetry:
  supported: true
  supported_contract_versions:
    - telemetry-bundle-v1
  supported_scopes:
    - test-executor
    - load-generator
  supported_artifact_kinds:
    - log
    - metrics
  export_modes:
    - artifact-bundle
    - none
