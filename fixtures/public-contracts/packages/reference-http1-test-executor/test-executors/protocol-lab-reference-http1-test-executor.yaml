executorIdentity:
  id: protocol-lab-reference-http1-test-executor
  name: ProtocolLab Reference HTTP/1 Test Executor
  version: 0.1.0
  vendor: Incursa
versionCompatibility:
  contractVersion: test-executor-v1
  compatibleContractVersions:
    - test-executor-v1
  executorVersion: 0.1.0
claimedCapabilities:
  - id: http.core.plaintext
    status: supported
  - id: http.core.json
    status: supported
  - id: http.payload.bytes.1kb
    status: supported
supportedTestSelectors:
  - selectorType: test-id
    expression: http.core.plaintext
  - selectorType: test-id
    expression: http.core.json
  - selectorType: test-id
    expression: http.payload.bytes.1kb
supportedScenarioSelectors:
  - selectorType: scenario-id
    expression: http.core.plaintext
  - selectorType: scenario-id
    expression: http.core.json
  - selectorType: scenario-id
    expression: http.payload.bytes.1kb
supportedProtocolFamilies:
  - h1
supportedExecutionModes:
  - single-cell
  - process
requiredTargetEndpointBindings:
  - bindingId: primary
    purpose: test-endpoint
    endpointType: http
    protocols:
      - h1
supportedArtifactTypes:
  - type: validation.json
    producedByStates:
      - stopped
      - failed
  - type: result.json
    producedByStates:
      - stopped
      - failed
  - type: load-tool.stdout.txt
    producedByStates:
      - stopped
      - failed
  - type: load-tool.stderr.txt
    producedByStates:
      - stopped
      - failed
  - type: load-tool-execution.json
    producedByStates:
      - stopped
      - failed
metricsAvailability:
  available: true
  availableKinds:
    - session
    - validation
