schemaVersion: "1.0"
id: webtransport.session-open
title: WebTransport Session Open
name: WebTransport Session Open
family: webtransport
description: Placeholder for WebTransport session-open scenario. Not yet implemented.
status: placeholder
kind: protocolValidation
layer: application
protocol: h3
roles:
  - server
requires:
  capabilities:
    - webtransport.server
  protocols:
    - h3
  roles:
    - server
trafficShape: bidirectional-stream
validation:
  required: true
  checks:
    - unsupported-until-webtransport-validator
benchmark:
  warmupSeconds: 5
  durationSeconds: 30
  repetitions: 1
  connections:
    - 1
  streamsPerConnection:
    - 1
benchmarkCompat:
  compatibleLoadShapes: []
  primaryMetrics:
    - webtransport.session.durationMs
artifacts:
  required:
    - validation.json
  optional:
    - protocol-diagnostic-artifact
networkProfile: clean
artifactRequirements:
  - load-generator.stdout.txt
  - load-generator.stderr.txt
tags:
  - phase8
  - webtransport
  - future-family
  - placeholder
