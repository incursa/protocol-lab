executorIdentity:
  id: protocol-lab-reference-http1-test-executor
  name: ProtocolLab Reference HTTP/1 Test Executor Fixture
  version: 0.1.0-fixture
  vendor: Incursa
versionCompatibility:
  contractVersion: test-executor-v1
  compatibleContractVersions:
    - test-executor-v1
  executorVersion: 0.1.0-fixture
claimedCapabilities:
  - id: http1.core.plaintext
    status: supported
  - id: http1.core.json
    status: supported
  - id: http1.payload.bytes.1kb
    status: supported
supportedTestSelectors:
  - selectorType: test-id
    expression: http1.core.plaintext
  - selectorType: test-id
    expression: http1.core.json
  - selectorType: test-id
    expression: http1.payload.bytes.1kb
supportedScenarioSelectors:
  - selectorType: scenario-id
    expression: http1.core.plaintext
  - selectorType: scenario-id
    expression: http1.core.json
  - selectorType: scenario-id
    expression: http1.payload.bytes.1kb
supportedProtocolFamilies:
  - h1
supportedExecutionModes:
  - contract-declared
requiredTargetEndpointBindings:
  - bindingId: primary
    purpose: test-endpoint
    endpointType: http
    protocols:
      - h1
supportedArtifactTypes:
  - type: validation.json
    producedByStates:
      - stopped
      - failed
  - type: result.json
    producedByStates:
      - stopped
      - failed
  - type: load-generator.stdout.txt
    producedByStates:
      - stopped
      - failed
  - type: load-generator.stderr.txt
    producedByStates:
      - stopped
      - failed
metricsAvailability:
  available: true
  availableKinds:
    - session
    - validation
telemetry:
  supported: true
  supported_contract_versions:
    - telemetry-bundle-v1
  supported_scopes:
    - test-executor
    - load-generator
  supported_artifact_kinds:
    - log
    - metrics
  export_modes:
    - artifact-bundle
    - none
