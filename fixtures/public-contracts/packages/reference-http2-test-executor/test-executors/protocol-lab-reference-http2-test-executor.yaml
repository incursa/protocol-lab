executorIdentity:
  id: protocol-lab-reference-http2-test-executor
  name: ProtocolLab Reference HTTP/2 Test Executor Fixture
  version: 0.1.0-fixture
  vendor: Incursa
versionCompatibility:
  contractVersion: test-executor-v1
  compatibleContractVersions:
    - test-executor-v1
  executorVersion: 0.1.0-fixture
claimedCapabilities:
  - id: http2.core.plaintext
    status: supported
  - id: http2.core.json
    status: supported
  - id: http2.streaming.response
    status: supported
supportedTestSelectors:
  - selectorType: test-id
    expression: http2.core.plaintext
  - selectorType: test-id
    expression: http2.core.json
  - selectorType: test-id
    expression: http2.streaming.response
supportedScenarioSelectors:
  - selectorType: scenario-id
    expression: http2.core.plaintext
  - selectorType: scenario-id
    expression: http2.core.json
  - selectorType: scenario-id
    expression: http2.streaming.response
supportedProtocolFamilies:
  - h2
supportedExecutionModes:
  - contract-declared
requiredTargetEndpointBindings:
  - bindingId: primary
    purpose: test-endpoint
    endpointType: http
    protocols:
      - h2
supportedArtifactTypes:
  - type: validation.json
    producedByStates:
      - stopped
      - failed
  - type: result.json
    producedByStates:
      - stopped
      - failed
  - type: load-generator.stdout.txt
    producedByStates:
      - stopped
      - failed
  - type: load-generator.stderr.txt
    producedByStates:
      - stopped
      - failed
metricsAvailability:
  available: true
  availableKinds:
    - session
    - validation
telemetry:
  supported: true
  supported_contract_versions:
    - telemetry-bundle-v1
  supported_scopes:
    - test-executor
    - load-generator
  supported_artifact_kinds:
    - log
    - metrics
  export_modes:
    - artifact-bundle
    - none
