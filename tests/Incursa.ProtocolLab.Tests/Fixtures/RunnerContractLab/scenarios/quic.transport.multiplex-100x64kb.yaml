id: quic.transport.multiplex.100x64kb
name: QUIC Multiplex 100x64KB
family: quic.transport
version: "1.0"
description: Fixture-root copy of the enabled raw QUIC multiplex transport scenario.
status: stable
kind: protocolValidation
layer: transport
protocol: quic
implementationRole: server
trafficShape: bidirectional-stream
requiredCapabilities:
  - quicTransport
  - quicStreams
  - quicMultiplexing
quicTransport:
  behavior: multiplex-streams
  connectionCount: 1
  streamType: bidirectional
  streamCount: 100
  payloadSizeBytes: 65536
  payloadDirection: bidirectional
  openPattern: concurrent
  expectedBytes: 13107200
validation:
  required: true
  checks:
    - raw-quic-load-tool-metrics
benchmark:
  warmupSeconds: 0
  durationSeconds: 1
  repetitions: 1
  connections:
    - 1
  streamsPerConnection:
    - 100
networkProfile: clean
tags:
  - fixture
  - quic
